# Frontend environment (Vite). All vars are build/dev-time only — no secrets.
#
# Backend target for the dev server proxy (vite.config.ts). In Docker/prod the
# same prefixes are reverse-proxied by nginx instead (frontend/nginx.conf), so
# this is only consulted by `npm run dev`.
VITE_BACKEND_URL=http://localhost:8080
